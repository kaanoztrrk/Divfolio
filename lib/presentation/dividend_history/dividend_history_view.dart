import 'package:flutter/material.dart';

class DividendHistoryView extends StatelessWidget {
  const DividendHistoryView({super.key});
  @override
  Widget build(BuildContext context) =>
      const Center(child: Text('Dividend History'));
}
